#!/bin/sh
# DESCRIPTION:  it is used to allow either hsic or hsusb to have no composition at all(must reboot to take effect).
chmod 777 /sys/devices/f9200000.ssusb/power_supply/usb/present
echo 1 > /sys/devices/f9200000.ssusb/power_supply/usb/present

